{
  "name": "books-backend-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for books API (mock data + Hyperdrive)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "private": true,
  "dependencies": {
    "postgres": "3.4.5"
  },
  "devDependencies": {
    "wrangler": "4.21.x"
  }
} 